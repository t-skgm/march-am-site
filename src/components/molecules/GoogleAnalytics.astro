---
export interface Props {
  gtagId: string
}
const { gtagId } = Astro.props

const gtagUrl = `https://www.googletagmanager.com/gtag/js?id=${gtagId}`
---

<!-- Global site tag (gtag.js) - Google Analytics -->
<script async src={gtagUrl}></script>
<script define:vars={{ gtagId }}>
  window.dataLayer = window.dataLayer || []
  function gtag() {
    // oxlint-disable-next-line prefer-rest-params -- gtag.js は Arguments オブジェクトを要求する
    dataLayer.push(arguments)
  }
  gtag('js', new Date())
  gtag('config', gtagId)
</script>
